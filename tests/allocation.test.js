import test from 'node:test';import assert from 'node:assert/strict';import { allocation, mapTransaction } from '../server.js';
test('monthly OPEN allocation follows approved threshold',()=>{assert.equal(allocation(0),0);assert.equal(allocation(10),50);assert.equal(allocation(50),250);assert.equal(allocation(60),260);assert.equal(allocation(100),300);assert.equal(allocation(250),450)});
test('integration mapping applies one processor fee and approved pack split',()=>{const item={type:'mystery_pack',price:29.99};const mapped=mapTransaction({item,quantity:1,channel:'in_person',monthlyUnitNumber:1});assert.equal(mapped.gross,29.99);assert.equal(mapped.processorFee,0.93);assert.equal(mapped.net,29.06);assert.equal(mapped.openPayable,6.45);assert.equal(mapped.acoolProceeds,22.61)});
test('pack base allocation drops after monthly unit 50',()=>{const item={type:'mystery_pack',price:49.99};const mapped=mapTransaction({item,quantity:2,channel:'online',monthlyUnitNumber:50});assert.equal(mapped.processorFee,3.2);assert.equal(mapped.openPayable,10.84);assert.equal(mapped.acoolProceeds,85.94)});
